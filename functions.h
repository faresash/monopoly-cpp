/*#include <iostream>
#include <string>


int GetIndex(std::string squareName) {
    if (squareName == "Go") {
        return 0;
    }

    // RAILROADS
    if (squareName == "READING_RAILROAD") {
        return 5;
    }

    if (squareName == "PENNSYLVANIA_RAILROAD") {
        return 15;
    }

    if (squareName == "B.&O._RAILROAD_RAILROAD") {
        return 25;
    }

    // UTILITIES
    if (squareName == "ELECTRIC_COMPANY") {
        return 12;
    }

    // BROWNS
    if (squareName == "MEDITERRANEAN_AVENUE") {
        return 1;
    }

    if (squareName == "BALTIC_AVENUE") {
        return 3;
    }

    // LIGHT BLUES
    if (squareName == "ORIENTAL_AVENUE") {
        return 6;
    }

    if (squareName == "VERMONT_AVENUE") {
        return 8;
    }

    if (squareName == "CONNECTICUT_AVENUE") {
        return 9;
    }

    // PINKS
    if (squareName == "ST.CHARLES_PLACE") {
        return 11;
    }

    if (squareName == "STATES_AVENUE") {
        return 13;
    }

    if (squareName == "VIRGINIA_AVENUE") {
        return 14;
    }

    //
}

 */